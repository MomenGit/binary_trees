#include "binary_trees.h"
#include <stdlib.h>

/**
 * binary_tree_is_leaf - checks if a node is a leaf
 *
 * @node: a pointer to the node to check
 * Return: 1 if node is a leaf, or otherwise
 */
int binary_tree_is_leaf(const binary_tree_t *node)
{
}
